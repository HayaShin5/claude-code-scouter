{
  "name": "claude-code-scouter",
  "displayName": "Claude Code Scouter",
  "description": "Scans and displays danger level of commands Claude Code asks to execute",
  "version": "0.3.0",
  "icon": "src/assets/icon.jpg",
  "publisher": "HayaShin5",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/HayaShin5/claude-code-scouter"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claude-code-scouter.showDetails",
        "title": "Claude Code Scouter: Show Details"
      }
    ],
    "configuration": {
      "title": "Claude Code Scouter",
      "properties": {
        "claude-code-scouter.language": {
          "type": "string",
          "enum": ["auto", "en", "ja"],
          "default": "auto",
          "description": "Display language for danger level descriptions. 'auto' detects from VSCode locale."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "node --test out/test/**/*.test.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
